import { userSchema } from "./user";
import { helloSchema } from "./hello";
import { postSchema } from "./post";
import { parishSchema } from "./parish";

const schemas = [userSchema, helloSchema, postSchema, parishSchema];

export default schemas;
